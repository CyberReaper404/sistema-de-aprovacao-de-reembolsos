import { PlaceholderState } from "@/components/feedback/PlaceholderState";

export function DashboardPage() {
  return (
    <PlaceholderState
      eyebrow="Painel"
      title="Estrutura inicial do dashboard pronta"
      description="A base de navegação, sessão e integração já está preparada. O dashboard visual será construído na rodada específica da feature."
    />
  );
}
