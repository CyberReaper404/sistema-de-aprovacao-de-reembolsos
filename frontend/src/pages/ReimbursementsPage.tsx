import { PlaceholderState } from "@/components/feedback/PlaceholderState";

export function ReimbursementsPage() {
  return (
    <PlaceholderState
      eyebrow="Solicitações"
      title="Listagem principal preparada"
      description="A rota e os contratos de dados já existem. A tabela, os filtros e a paginação entram na rodada de listagem."
    />
  );
}
