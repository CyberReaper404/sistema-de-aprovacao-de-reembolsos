import { PlaceholderState } from "@/components/feedback/PlaceholderState";

export function AdminPage() {
  return (
    <PlaceholderState
      eyebrow="Administração"
      title="Módulo administrativo reservado"
      description="A navegação por papel já considera a administração. As telas específicas entram quando a frente administrativa do frontend começar."
    />
  );
}
